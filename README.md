# Script for automated installation of curl

## About the Project

This project provides a simple command‑line script that automatically installs *curl* on a Linux system. 

It checks whether curl is already installed and installs it if necessary, ensuring the tool is ready to use without manual intervention.

---

## Setup

Follow the steps below to set up the project:

1. Use a Linux machine.
2. Clone the repository using *HTTPS* or *SSH*:
   - HTTPS: git clone https://github.com/your/repo.git
   - SSH: git clone git@github.com:your/repo.git
3. Run the setup script:
   ```bash
   ./scripts/setup.sh

## Information

If everything okay, you needs the message:

# "A instalar curl"
